# Latex Resume Builder

This project is a structured resume generator that uses LaTeX for clean, professional document output. 

## Project Goal 
Allows users to enter resume information without writing LaTeX, choose a template, and generate a well-formatted resume automatically. 

## Roadmap 
- Convert resume schema into editable data objects
- Add LaTeX templates
- Render JSON data into LaTeX
- Compile LaTeX into PDF

